# 数据库设计指南 (Industrial Grade V2.4)

## 数据库概述

LOF 基金套利监控系统采用 **"大统一超级宽表"** 和 **"模块化职责分离"** 的数据库设计哲学。系统默认使用 SQLite 数据库，并强制开启 WAL (Write-Ahead Logging) 模式，以支持多线程高频写入与查询。

**数据库路径**：`d:\Study\arbTest\database\arb_master.db`（上级目录，所有项目共享）

**覆盖项目**：ArbDashboard（JSL、LOFarb 已废弃）

**已废弃**：`jsl/jsl_monitor.db`（2026-06-09 删除）

---

## 模块化管理器 (DatabaseManager)

为了提升代码的可维护性，数据库接口不再堆叠在单一类中，而是通过 `组合模式` 拆分为三个专业子管理器：

### 1. 基金管理器 (`db.funds`)
- **职能**：管理 A 股基金的价格、净值、估值因子及底层篮子权重。
- **核心表**：`unified_fund_list`, `unified_fund_history`, `fund_daily_factors`, `fund_basket_weights`

### 2. 市场管理器 (`db.market`)
- **职能**：管理宏观汇率、海外 ETF 收盘价、指数 K 线及期货结算价。
- **核心表**：`exchange_rate`, `usa_etf_daily_prices`, `futures_daily`, `index_daily`, `index_history`, `index_realtime_quotes`

### 3. 系统管理器 (`db.system`)
- **职能**：维护 API 访问状态、系统健康日志及原始数据备份（Data Lake）。
- **核心表**：`access_sync_status`, `system_health`, `raw_api_data`, `data_source_config`

---

## 核心数据字典

### 1. unified_fund_list (统一基金列表)
*系统中所有基金的元数据配置表*

| 字段名 | 类型 | 说明 |
|--------|------|------|
| fund_code | TEXT | 基金代码（主键） |
| fund_name | TEXT | 基金名称 |
| category | TEXT | 分类（黄金原油、QDII欧美等） |
| related_index | TEXT | 跟踪指数 |
| pos_ratio | REAL | 仓位比例（默认 0.95） |

**当前数据**：88 只基金（2026-06-28）

### 2. unified_fund_history (统一基金历史)
*系统中最重要的超级宽表，每一行代表一只基金在一个交易日的完整成绩单。*

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 主键 |
| date | TEXT | 日期（YYYY-MM-DD，联合主键） |
| fund_code | TEXT | 基金代码（联合主键） |
| price | REAL | 盘中现价 / 盘后收盘价 |
| price_change | REAL | 价格变化 |
| nav | REAL | 官方日终净值 |
| nav_date | TEXT | 净值日期 |
| volume | REAL | 成交量 |
| shares | REAL | 份额 |
| shares_added | REAL | 新增份额 |
| turnover_rate | TEXT | 换手率 |
| static_val | REAL | 静态官方估值 |
| rt_val | REAL | 实时估值 |
| premium | REAL | 盘后溢价率 |
| rt_premium | REAL | 实时溢价率 |
| index_close | REAL | 指数收盘价 |
| index_pct | REAL | 指数变化 |
| calibration | REAL | 校准值 |
| purchase_status | TEXT | 申购状态 |
| redemption_status | TEXT | 赎回状态 |
| valuation_error | REAL | 估值错误 |
| premium_error | REAL | 溢价错误 |
| created_at | TIMESTAMP | 创建时间 |

**当前数据**：19,962+ 条记录（2026-06-28）

### 3. fund_daily_factors (估值因子表)
*Woody API 提取后的常量池，是前端秒开渲染的基石。*

| 字段名 | 类型 | 说明 |
|--------|------|------|
| fund_code | TEXT | 基金代码 |
| calibration | REAL | 校准值 (Calibration) |
| hedge | REAL | 对冲比例 (Hedge) |
| position | REAL | 基础仓位 (Position) |
| nav | REAL | 参考净值 |

### 4. usa_etf_daily_prices (海外 ETF 价格表)
*记录美股底层标的的基准价格，特别支持"虚拟后缀"替身机制。*

| 字段名 | 类型 | 说明 |
|--------|------|------|
| date | TEXT | 日期（联合主键） |
| symbol | TEXT | 标的代码（如 GLD, ^GLD-EU）（联合主键） |
| price | REAL | 收盘价（或特时点快照价） |
| netvalue | REAL | 官方净值（若有） |
| updated_at | TIMESTAMP | 更新时间 |

### 5. exchange_rate (汇率表)
*记录 USD/CNY、HKD/CNY 等汇率数据。*

| 字段名 | 类型 | 说明 |
|--------|------|------|
| date | TEXT | 日期（主键） |
| usd_cny_mid | REAL | 美元兑人民币汇率 |
| hkd_cny_mid | REAL | 港币兑人民币汇率 |
| updated_at | TIMESTAMP | 更新时间 |

**当前数据**：133 条记录（2026-06-28）

---

## 性能优化与设计哲学

1. **动静结合 (Upsert Strategy)**：
   - 盘中使用 `INSERT OR REPLACE` 实时覆写当天行，保持数据新鲜。
   - 盘后通过 `UPDATE` 精修官方字段（如 `nav`），实现无缝过渡。

2. **快慢分离**：
   - 指数行情分为"实时快照表"（单行缓存）与"历史 K 线表"（分段归档）。

3. **安全隔离**：
   - ETF 轮动模块使用物理隔离的 `etf_raw_api_data` 表，确保策略私密性。

4. **全自动清理**：
   - `db.cleanup_old_data(days=30)` 自动回收陈旧的常规行情数据，防止数据库膨胀。

---

## 数据迁移记录

### 2026-06-28：清理程序2遗留表，fund_data 等 4 表删除

**背景**：`fund_data` 表是程序2（JSL/集思录）遗留的旧价格表，所有读写路径已迁移到 `unified_fund_history`，`fund_data` 不再需要。

**删除的表**：

| 表名 | 行数 | 说明 |
|------|------|------|
| `fund_data` | 12,560 | 旧价格表，全部读写已迁移到 `unified_fund_history` |
| `fund_history` | 33,060 | 旧历史表，已被 `unified_fund_history` 替代 |
| `fund_info` | 72 | 元数据表，最后读取已迁移到 `unified_fund_list` |
| `jsl_fund_list` | 22 | 集思录基金列表，无调用者 |

**数据库变化**：28 表 → 24 表

### 2026-06-09：JSL 项目数据库统一

**迁移前**：
- JSL 项目使用独立数据库：`jsl/jsl_monitor.db`
- ArbDashboard 使用统一数据库：`arb_master.db`

**迁移后**：
- 所有项目统一使用：`d:\Study\arbTest\database\arb_master.db`
- 迁移数据：
  - 72 只基金元数据（fund_info → unified_fund_list）
  - 37,675 条历史记录（fund_history → unified_fund_history）
  - 107 条汇率数据（exchange_rate → exchange_rate）

**修改文件**：
1. `jsl/00_web_server.py` - 4 处修改
2. `jsl/03_eod_price_nav_index.py` - 1 处修改
3. `jsl/05_eod_history_valuation.py` - 1 处修改
4. `jsl/042_jsl_sync_shares.py` - 1 处修改

**废弃文件**：
- `jsl/jsl_monitor.db` - 已删除

---

*本文档由 Gemini 数据库重构模块自动生成，V2.4 更新于 2026-06-28*
