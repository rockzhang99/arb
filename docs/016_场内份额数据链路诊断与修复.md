# 场内份额数据链路诊断与修复报告

> 调查日期：2026-06-17（初诊）, 2026-07-04（再诊 + 修复）
> 问题：程序3（ArbDashboard）所有TAB所有基金的场内份额几乎都不正确，大量缺失数据，有数据的也很多不对

---

## 一、问题现象

1. **34 只基金完全没有份额数据**：511880、511360、511520、501025、501301、501310 等上交所基金全部为 0
2. **有数据的基金也大量缺失历史**：例如 501018 从 06-15 才开始有份额数据，之前全部为空
3. **前端显示的份额/新增/换手率列大面积空白**

---

## 二、数据链路全图

```
VPS (06:00 每日)
  └─ 041_jsl_cloud_shares.py (cron)
       ├─ 深交所 API: szse.cn → 逐只抓取 (2-4s 间隔/只) [AI-2026-07-04: 改用 requests.Session]
       └─ 上交所 API: sse.com.cn → 批量获取 119 只
       ↓
   shares_YYYY-MM-DD.json (存于 VPS_DATA_DIR/)
       ↓
   SFTP 拉取到本地 ArbDashboard/data/vps_sync/
       ↓
  本地 daily_updater.py step9 (SFTP 拉取)
       ↓
   unified_fund_history.shares 字段
       ↓
   fund_service.py → 前端 Dashboard.vue
```

---

## 三、根因分析（2026-06-17 初诊）

### Bug 1：VPS 抓取范围不足（已修复 2026-06-17）

**现象**：88 只基金中只有 50 只有份额数据，34 只完全缺失

**根因**：VPS 上的 `jsl_vps_symbols.txt` 只包含 50 只深交所基金 + 1 只上交所基金（sh501018）。上交所的 511880、511360、511520、501025 等全部不在抓取列表中。

**证据**：
```
VPS jsl_vps_symbols.txt 旧内容（2行）:
161716,168204,...,165513   (49只深交所)
,sh501018                   (1只上交所)
```

VPS 脚本 `041_jsl_cloud_shares.py` 实际上**已经内置了上交所批量获取能力**（`fetch_sse_shares_all()` 函数，一次获取 119 只 LOF 份额），但 symbols 列表中没有包含上交所基金代码，导致这些基金从未被采集。

**修复**：将 `jsl_vps_symbols.txt` 扩展为 81 只基金（55 深交所 + 26 上交所），已上传 VPS。

### Bug 2：本地 step9 同步逻辑过于保守（已修复 2026-06-17）

**现象**：即使 VPS 有历史数据，本地也经常不拉取

**根因**：step9 有两个过于激进的 guard：

```python
# Guard 1: 今日已标记就跳过（一旦标记永不回头）
if self.db.is_access_synced_today(today_str, source='jsl_shares_data'):
    return

# Guard 2: 昨日数据已在库就跳过（但今天的数据没入库）
if count > 0:  # 昨日份额已在库
    return
```

Guard 2 尤其有害：如果 06-16 的份额已在库，step9 就认为"份额已同步"直接跳过，但 06-17 的份额根本没拉取。

**修复**：重写 step9，去掉两个 guard，改为逐文件检查：对每个 VPS 文件，检查 DB 中该日期是否已有 >= 30 条份额数据，没有才入库。

### Bug 3：日期对齐问题（待确认）

**现象**：VPS 文件名是采集日期（如 `shares_2026-06-17.json`），但 06:00 采集时交易所尚未开盘

**分析**：
- VPS 06:00 采集 → 交易所 API 返回的是 T-1 日收盘后的份额（因为今天还没开盘）
- step9 按 `file_date`（= 采集日期）写入 DB → 实际写入了 T-1 的数据到 T 的日期

**实际影响**：目前影响不大，因为份额数据变化缓慢（日变化 < 1%）。

**修复**：[AI-2026-07-04] 已在 step9 中实现日期对齐——VPS 文件名是采集日期（如 `shares_2026-06-17.json`），写入 DB 时自动减一天（即 `2026-06-16`）。

---

## 四、2026-07-04 再诊 — 份额断供问题

### 新发现

**症状**：06-29 之后，VPS 份额文件从 1700 字节骤降到 126 字节，仅含 6 只港股 QDII 亚洲基金。

**根因分析**：

1. **`deploy_vps.py` 从 `jsl/fund_list.csv` 读取 symbols**，但该文件已不存在（用户 2026-07-03 移除了 `jsl/` 目录）
2. 导致 `deploy_vps.py` 从未上传 `jsl_vps_symbols.txt` 到 VPS
3. VPS 上的旧 symbols 文件可能已过期或被清空
4. VPS 脚本 `041_jsl_cloud_shares.py` 用 `urllib.request` 逐只请求深交所 API，缺少 `Host` 头，可能被深交所反爬机制拦截
5. 上交所批量接口 (`fetch_sse_shares_all()`) 正常返回，但只匹配到 6 只港股基金

### 修复措施（2026-07-04）

| 修改 | 文件 | 变更 |
|------|------|------|
| 从 lof_config.yaml 重建 symbols | `deploy/deploy_vps.py` | 不再依赖 `jsl/fund_list.csv`，从 YAML 提取所有基金代码 |
| 改用 requests.Session | `deploy/041_jsl_cloud_shares.py` | 与本地 `04_eod_shares.py` 一致，保持连接、添加 `Host` 头、`verify=False` |
| 质量检查 | `daily_updater.py step9` | 当天文件基金数 < 20 时发出警告 |

### 操作步骤

1. **重新部署 VPS**：运行 `python deploy/deploy_vps.py` 上传修复后的 symbols 文件和采集脚本
2. **手动触发 VPS 重跑**：SSH 到 VPS 执行 `/usr/bin/python3 /root/LOFarb/041_jsl_cloud_shares.py --file /root/LOFarb/jsl_vps_symbols.txt --outdir /root/ArbWebApp/siphon/data/`
3. **本地同步**：运行 `python ArbDashboard/backend/scheduler/daily_updater.py` 拉取补全历史数据

---

## 五、已执行的修复

### 5.1 VPS 端（2026-06-17）

| 操作 | 状态 | 说明 |
|------|------|------|
| 更新 `jsl_vps_symbols.txt` | ✅ 已完成 | 从 50 只扩展到 81 只（55 SZ + 26 SH） |
| 旧文件备份 | ✅ 已完成 | `jsl_vps_symbols.txt.bak` |
| 手动触发 VPS 重跑 041 脚本 | ✅ 运行中 | 使用新 symbols 列表，约 5 分钟完成 |

### 5.2 本地端（2026-06-17）

| 操作 | 状态 | 说明 |
|------|------|------|
| 重写 `step9_fetch_jsl_shares_from_vps()` | ✅ 已完成 | 去掉过于保守的 guard，改为增量补全 |
| 清除旧 sync 标记 | ✅ 已完成 | 原来无旧标记（说明 guard 从未阻止过 step9） |
| 运行 step9 补全历史数据 | ⏳ 待执行 | 需要等 VPS 重新抓取完成后运行 |

### 5.3 VPS 端（2026-07-04 追加修复）

| 操作 | 状态 | 说明 |
|------|------|------|
| 更新 `deploy_vps.py` | ✅ 已完成 | 从 lof_config.yaml 提取 symbols，不再依赖 fund_list.csv |
| 更新 `041_jsl_cloud_shares.py` | ✅ 已完成 | 改用 requests.Session + Host 头 + verify=False |
| 添加 step9 质量检查 | ✅ 已完成 | 当天文件基金数 < 20 时发出警告 |
| 重新部署 VPS | ⏳ 待执行 | 运行 `python deploy/deploy_vps.py` |
| 手动触发 VPS 份额重跑 | ⏳ 待执行 | SSH 到 VPS 执行采集脚本 |

---

## 六、VPS 脚本架构

### `041_jsl_cloud_shares.py`（VPS 端）

**部署位置**：`/root/LOFarb/041_jsl_cloud_shares.py`
**Cron 触发**：`0 6 * * 1-5`（工作日 06:00）
**输入**：`/root/LOFarb/jsl_vps_symbols.txt`（逗号分隔，SH 基金加 `sh` 前缀）
**输出**：`/root/LOFarb/siphon_data/shares_YYYY-MM-DD.json`

**采集逻辑**：
1. 深交所基金（16xxxx）：逐只调用 `szse.cn/api/report/ShowReport/data` 接口，每只间隔 2-4 秒（反爬）
2. 上交所基金（5xxxxx + sh 前缀）：调用 `query.sse.com.cn/commonQuery.do` 批量获取 119 只 LOF 份额，一次搞定
3. 两个交易所的数据合并为一个 JSON 文件

**关键字段**：
- 深交所：`dqgm`（当前规模，单位万份）
- 上交所：`INTERNAL_VOL`（内部规模，单位万份）

### `daily_updater.py step9`（本地端）

**调用链**：
```
step9_fetch_jsl_shares_from_vps()
  → _try_sync_all_from_vps('shares')
    → SFTP 列出 VPS /root/LOFarb/siphon_data/shares_*.json
    → 下载到本地 data/vps_sync/ 目录
    → 返回 [{date, content}, ...]
  → 逐文件检查 DB 中该日期份额数量
  → 缺失的写入 unified_fund_history.shares
```

---

## 七、验证数据

### VPS 数据正确性（调查时确认）

```
162411 shares across days:
  06-12: 184658.05
  06-15: 184422.20
  06-16: 184018.98
  06-17: 183540.13   ← 每天都在变化，无重复

0 duplicated values across 4 days for all 50 funds
```

VPS 数据本身是正确的，问题在于抓取范围不足。

### DB 覆盖率（修复前）

| 指标 | 修复前 |
|------|--------|
| 总行数 | 9282 |
| 有份额的行 | 1022 (11%) |
| 有份额的基金 | 51/81 |
| 完全无份额的基金 | 34 只（主要是上交所） |

---

## 八、明日待办

1. **确认 VPS 重跑完成**：检查 `shares_2026-06-17.json` 是否包含 81 只基金
2. **运行 step9 补全历史**：清除 sync 标记后运行 daily_updater，让 13 个历史 VPS 文件全部入库
3. **验证 DB 覆盖率**：确认所有 81 只基金都有最近 10 天的份额数据
4. **确认日期对齐**：如果 06:00 采集的是 T-1 数据，需要修改 step9 的日期逻辑
5. **重启后端**：让 fund_service 读取最新的份额数据
6. **修复后端 API 返回 HTML 的问题**：当前后端在 port 8000 返回 SPA HTML 而非 JSON，可能是静态文件 mount 拦截了 API 路由（检查 main.py 中 static mount 的顺序）

---

## 九、关键文件索引

| 文件 | 位置 | 说明 |
|------|------|------|
| `041_jsl_cloud_shares.py` | VPS: `/root/LOFarb/` | VPS 端份额采集脚本 |
| `jsl_vps_symbols.txt` | VPS: `/root/LOFarb/` | 基金代码列表（已更新为 81 只） |
| `daily_updater.py` step9 | `ArbDashboard/backend/scheduler/daily_updater.py:932` | 本地份额同步（已重写） |
| `_try_sync_all_from_vps()` | `daily_updater.py:66` | VPS 数据通用同步函数 |
| `fund_service.py` L459-468 | `ArbDashboard/backend/services/fund_service.py` | 份额数据读取和 shares_added 计算 |
| `Dashboard.vue` L336-346 | `ArbDashboard/frontend/src/views/Dashboard.vue` | 份额列前端显示 |
| `fund_manager.py:202` | `arbcore/database/managers/fund_manager.py` | `save_unified_history()` INSERT ON CONFLICT |
| `deploy_vps.py` | `deploy/deploy_vps.py` | VPS 部署脚本（已修复：从 lof_config.yaml 提取 symbols） |

---

*调查者：Sisyphus (AI) | 2026-06-17 23:00*
*再诊修复：2026-07-04*
