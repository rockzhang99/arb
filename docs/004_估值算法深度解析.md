# 估值算法深度解析：从 Woody 源码到本系统实现

> 本文档深入剖析 woody 系统的估值算法原理，以及本系统如何复刻和优化这些算法。最后更新：2026-06-08

---

## 目录

1. [Woody 系统的三种估值](#1-woody-系统的三种估值)
2. [校准值的数学本质](#2-校准值的数学本质)
3. [Hedge（对冲值）的物理意义](#3-hedge对冲值的物理意义)
4. [常量折叠：Woody 的数学魔法](#4-常量折叠woody-的数学魔法)
5. [本系统的三种实时估值实现](#5-本系统的三种实时估值实现)
6. [三轨合一：数学等价性证明](#6-三轨合一数学等价性证明)
7. [对冲数量计算的双轨推演算法](#7-对冲数量计算的双轨推演算法)
8. [取价策略：卖一 vs 买一](#8-取价策略卖一-vs-买一)
9. [校准值的采样机制](#9-校准值的采样机制)

---

## 1. Woody 系统的三种估值

在 woody 里，"三种估值"对应的是三个 getter（名称一致，但实现因基金类型不同而不同）：

- **`GetOfficialNetValue()`**：官方估值（更准确地说，是用官方净值日期/官方收盘数据为锚点算出来的估值），对应本系统的**静态 ETF 估值**
- **`GetFairNetValue()`**：参考估值（用更靠近当前时刻的价格/汇率重算一次，并加上一个"官方校正项"保证与官方锚点连续）
- **`GetRealtimeNetValue()`**：实时估值（当底层参考标的存在"实时替身"（配对交易模型）时，用替身推算价格，再做一次 Fair 的那套逻辑），对应本系统的**实时 ETF 估值**

这三者在源码里主要有两条路径：

1. **QDII 单参考标的型**（"基金 ↔ 指数/ETF"的直接映射）：`php/stock/qdiiref.php`
2. **QDII 多持仓/混合型**（"基金持仓由多个 ETF/股票构成"）：`php/stock/holdingsref.php`

### 1.1 Holdings 型（多持仓）——以 SZ160719 黄金 LOF 为代表

源码：`php/stock/holdingsref.php`

#### (1) OfficialNetValue：官方估值（锚定官方净值日）

`HoldingsReference::GetOfficialNetValue()` 的核心流程：

1. 选择一个"官方锚定日期" `officialDate`（逻辑在 `GetOfficialDate()`，会考虑 A 股基金/外汇收盘是否可用等）
2. 计算一个"模型估值" `modelOfficial = _estNetValue(officialDate, bRealtime=false)`
3. 读取数据库里该日真正的基金净值 `navOfficial = netvaluehistory[officialDate]`（如果存在）
4. 记录一个"官方校正项"：

```
officialAdjust = navOfficial - modelOfficial
```

并把 `modelOfficial` 写到 `fundest`（用于展示/回溯）。

> **注意**：OfficialNetValue 在这里是"模型估值"（`modelOfficial`），不是直接等于基金公司发布的 `navOfficial`。`navOfficial` 被用来形成 `officialAdjust`，从而让后续 Fair/Realtime 估值在数值上与官方锚点"对齐且连续"。

#### (2) FairNetValue：参考估值（用更新的价格/汇率重算，再加校正项）

`HoldingsReference::GetFairNetValue()`：当它判断"确实需要更实时"（比如外汇/持仓标的日期已经更新）时：

1. 先确保已算出 Official（拿到 `officialAdjust`）
2. 用当前时刻的数据再算一次：

```
modelFair = _estNetValue(date=false, bRealtime=false)   // 用当下价格/汇率
fair = modelFair + officialAdjust
```

因此 Fair 并不是"只做汇率替换"的简化公式，而是**完整重算持仓变化**，最后再用 `officialAdjust` 对齐官方锚点。

#### (3) RealtimeNetValue：实时估值（把"可实时推算的持仓价格"替换掉）

`HoldingsReference::GetRealtimeNetValue()`：只有当 `UseRealtimeEst()` 为真（也就是持仓里存在 `FundPairReference` 这种可用配对模型推算实时价的标的）才会启用。

它与 Fair 的区别只有一处：重算时传 `bRealtime=true`，对持仓里的某些标的用 `EstFromPair()` 替代直接取 `GetPrice()`：

```
modelRealtime = _estNetValue(date=false, bRealtime=true)
realtime = modelRealtime + officialAdjust
```

**一句话总结**：Realtime 不是"另一套公式"，而是"同一套持仓估值公式 + 用配对模型替换部分输入价格"。

### 1.2 QDII 单参考标的型（直接映射）

源码：`php/stock/qdiiref.php`

它的核心是一个因子 `fFactor`：

- 当"官方净值"当天准备好时，会用 `AdjustFactor()` 重新校准 `fFactor`，使模型与真实净值对齐：

```
fFactor = est * CNY_close / fund_nav
```

（源码函数：`QdiiGetCalibration($est, $cny, $netValue)`）

- 之后任何估值都走：

```
val = (est * CNY) / fFactor
val = AdjustPosition(val)    // 用仓位与基准净值做线性混合
```

其中 `est` 的取法分三种：

- **Official**：优先用数据库里该日的 `est_ref` 净值/收盘（或 holdings 估值）
- **Fair**：当 `est_ref` 日期或汇率日期已变动，用更新后的 `est`
- **Realtime**：如果 `est_ref` 自己也有"配对模型"（`GetRealtimeRef()` 存在），则用 `est_ref->EstFromPair()` 得到更实时的 `est`

---

## 2. 校准值的数学本质

### 2.1 校准值（factor）的计算公式

源码入口：`php/stock/fundpairref.php` 的 `FundPairReference::CalcFactor($pair, $net, $date)`：

1. 先取配对标的价格（或净值）：`fPair = float(pair)`
2. 再取本标的价格（或净值）：`fNet = float(net)`
3. 如有跨币种（存在 `$this->cny_ref`），会把 `fNet` 换算到与 `fPair` 同币种后再比（方向由是否 A 股决定）
4. 返回：

```
factor = fPair / fNet
```

**示例**：`GLD ↔ hf_GC`（USD 对 USD，无汇率换算）：

```
factor(GLD) = Price(hf_GC) / Price(GLD)
```

### 2.2 校准值的物理意义

**校准值的本质**：每 1 份 LOF 净值里，包含了多少价值的底层标的。

```
Calibration = (基准ETF单价 × 基准日汇率) / 基准日LOF净值
```

**物理意义**：每 1 份 LOF 净值里，包含了多少人民币价值的底层 ETF。

---

## 3. Hedge（对冲值）的物理意义

### 3.1 Hedge 的定义

在常量折叠的化简公式中，后半部分的系数是 `仓位 / Calibration`。

Woody 将这个倒数单独提炼出来，通过 API 传递给了我们，命名为 **Hedge（对冲值）**：

```
Hedge = Calibration / 仓位
```

### 3.2 Hedge 的绝对物理意义

**它代表的是纯粹的物理兑换比例！即：1 股美股 ETF，恰好等于 Hedge 股 A股 LOF！**

### 3.3 实时估值的终极简化公式

将 Hedge 代入，实时估值公式变成了极致简化的终极形态：

```
实时估值 = 基准净值 × (1 - 仓位) + (实时ETF × 实时汇率) / Hedge
```

**正如 Woody 原话中所说：**
> "前半部分是不变的现金仓位，后半部分是变化的股票持仓。API接口的使用者只需要自行拿到XOP的价格（注：即实时ETF），就能计算对应的SZ162411净值EST。"

---

## 4. 常量折叠：Woody 的数学魔法

### 4.1 原始长公式的痛点

我们常规的实时估值推导公式是：

```
实时估值 = 基准净值 × [ 1 + 仓位 × (实时ETF × 实时汇率 / (基准ETF × 基准汇率) - 1) ]
```

在这个公式里，盘中运算必须时刻知道"基准ETF"和"基准汇率"。这让前端计算变得非常臃肿，需要时刻携带昨天的历史账本。

### 4.2 Woody 的降维：Calibration（校准值）

Woody 发现，在 T 日盘中运算时，**基准净值、基准ETF、基准汇率，这三个数全都是昨天就已经定死的常量！**

于是他定义了一个降维常量 **Calibration（校准值）**：

```
Calibration = (基准ETF × 基准汇率) / 基准净值
```

代入原始公式化简后，分母奇迹般地完全抵消了：

```
实时估值 = 基准净值 × (1 - 仓位) + (仓位 / Calibration) × 实时ETF × 实时汇率
```

### 4.3 终极杀器：Hedge（对冲值）

将 Hedge 代入，实时估值公式变成了极致简化的终极形态：

```
实时估值 = 基准净值 × (1 - 仓位) + (实时ETF × 实时汇率) / Hedge
```

**常量折叠的优势**：
- 前端/后端只需要知道实时 ETF 价格 + Hedge 值
- 不需要携带基准日的 ETF 价格、基准日汇率等历史数据
- 计算复杂度从 O(n) 降为 O(1)

---

## 5. 本系统的三种实时估值实现

以上讲的是 woody 系统的估值逻辑。我们自己的系统（ArbDashboard）有三种实时估值，它们的核心公式和静态估值完全一样，区别在于**价格数据源不同**。

### ⚠️ 时差问题：仅QDII欧美基金

**只有投资美国市场的QDII基金（QDII欧美）面临两个时间问题：**

1. **净值滞后（T+1或更晚）**：
   - 基金公司在**T日收盘后**计算净值
   - 但由于时差和数据收集，T日的净值通常在**T+1日或更晚**才能获取
   - 例如：2026-06-08（周一）A股开盘时，只能拿到2026-06-07（周五）的净值

2. **交易时差**：
   - 中美交易时差约 **13-15 小时**
   - A股开盘时，美股已经收盘约15小时
   - 例如：2026-06-08（周一）A股9:30开盘时，美国2026-06-07（周五）15:00已收盘

**QDII欧美基金的估值都遵循：**

```
估值 = T-1日净值 × (1 + 仓位 × 标的涨跌幅 × 汇率变动)
```

**关键**：这里的"T-1日净值"必须是**最新可获得的净值**，可能是1天、2天甚至更久之前。

**QDII亚洲（161725、161726）没有中美时差问题**，但净值仍然滞后，必须仔细识别净值日期。

### 5.1 三种实时估值概览

| 实时估值列 | 位置 | 数据来源 | 适用场景 |
|-----------|-----|---------|---------|
| **ETF估值** | 最右边第一列 | 真实ETF实时价格 | 最准确，优先参考 |
| **期货校准** | 最右边第二列 | 期货实时价格 + 校准值 | 24小时有数据，美股休市时用 |
| **纯期货映射** | 最右边第三列 | 期货实时价格（直接用） | 解决ETF对冲流动性不足、滑点太大 |

### 5.2 ETF估值（最准确）

**数据来源**：真实的ETF实时价格（如GLD、USO、XOP等）

**计算公式**：

```
实时估值 = T-1日净值 × [1 + 仓位 × (ETF实时价/T-1日ETF价 × 汇率变动 - 1)]
```

**特点**：
- 和静态估值公式完全一样，只是把T日价格换成**实时价格**
- 通过JavaScript实时从新浪/IB获取数据更新
- 最准确，但15:00美股开盘前可能数据不完整

### 5.3 期货校准

**数据来源**：期货实时价格、校准值

**校准值来源**：
- 每天从woody网页爬取
- 不是拟合的，每天都在变
- 黄金校准值≈10.9，原油校准值≈0.82（示例值）

**核心步骤**：
1. 先用**校准值**把期货价格映射到ETF价格空间：
   ```
   等价ETF价 = 期货实时价 / 校准值
   ```
2. 然后用和**ETF估值一样的公式**计算

**计算公式**：

```
等价ETF价 = 期货实时价 / 校准值
实时估值 = T-1日净值 × [1 + 仓位 × (等价ETF价/T-1日ETF价 × 汇率变动 - 1)]
```

**特点**：
- 24小时都有期货数据，不会断
- 需要校准值准确才能保证估值准确

### 5.4 纯期货映射（解决ETF流动性问题）

**数据来源**：期货实时价格（直接用，不经过校准值）

**计算公式**：

```
实时估值 = T-1日净值 × [1 + 仓位 × (期货实时价/T-1日期货结算价 × 汇率变动 - 1)]
```

**注意**：
- 这里用的是**T-1日期货结算价**作为基准价，不是收盘价

**设计目的**：
- 解决ETF对冲时**流动性不足、滑点太大**的问题
- 纯期货市场流动性更好，滑点更小

**特点**：
- 最简单，不需要校准值
- 直接用期货价格作为底层价格
- 专为期货对冲设计，避免ETF的流动性问题

### 5.5 三种实时估值的适用场景

| 场景 | 推荐使用 | 原因 |
|-----|---------|-----|
| 美股交易时段 | ETF估值 | 最准确 |
| 美股休市时段 | 期货校准 | 24小时有期货数据 |
| 做期货对冲交易 | 纯期货映射 | 期货市场流动性好，滑点小 |
| 校准值不准时 | 纯期货映射 | 不需要校准值 |

---

## 6. 三轨合一：数学等价性证明

在实际盘中，对于纯指数类基金（如 161130 纳指、161125 标普），我们会观察到一个神奇的现象：**ETF实时估值、期货校准估值、纯期货映射估值，三者算出的溢价率往往高度一致甚至一模一样。**

这并非偶然，而是底层因子提取极其精确时展现出的**"数学等价性"**。

### 6.1 为什么"魔法公式" = "原始涨跌幅矩阵"？

如果我们把上一节推导出的 Hedge 物理定义代回魔法公式进行逆向展开：

```
Hedge = (基准日ETF单价 × 基准日汇率) / (基准日LOF净值 × 仓位)
```

将其代入魔法公式：

```
实时估值 = 基准净值 × (1 - 仓位) + (仓位 / Hedge) × (实时ETF × 实时汇率)
```

在纸上进行代数展开和约分（把基准日净值和仓位约掉），你会发现，它化简到最后恰恰就是最原始的涨跌幅矩阵公式：

```
实时估值 = 基准净值 × [ 1 + 仓位 × (实时ETF/基准ETF × 实时汇率/基准汇率 - 1) ]
```

**结论**：只要 Woody 提供的 Hedge 取值精确，不论是用简化版的"魔法公式"算，还是退回"T-1矩阵涨跌幅"算，数学上它们是同一个恒等式，结果必然完全一致。

### 6.2 为什么"期货校准" = "纯期货映射"？

纯期货的对比逻辑是：

```
实时估值变化 ∝ 实时期货 / 基准日期货
```

而期货校准的逻辑是：先把期货剔除升水变为现货，再去和基准日现货对比：

```
实时估值变化 ∝ (实时期货 / 校准升水率) / 基准日现货指数
```

由于基准日现货指数本身往往就等于 `基准日期货 / 校准升水率`。如果你把公式里分子分母同时除掉校准升水率（比如 1.003），你会发现 1.003 被完美约分抵消了！整个公式瞬间坍缩回了 `实时期货 / 基准日期货`。

**结论**：只有当基金面临换月展期，或者升贴水率突然发生巨变（比如从 1.003 暴增到 1.006）时，现货校准与纯期货的误差才会显现。在大部分平稳期，它们在数学上必然收敛于同一个值。

### 6.3 系统架构的最终抉择：大道至简

基于上述数学等价性，我们在处理指数类基金的"期货校准估值"时，坚决摒弃了"把实时期货转为现货，再用现货推导美股ETF，最后再套用魔法公式"的二次校准（脱裤子放屁）做法。

在本系统的代码实现中（如 LOF035 及 JS 运算逻辑），对于指数基金的期货校准：

1. **UI 层面**：贴心地计算并展示相当于多少**校准ETF测试价**，供用户直观参考。
2. **底层计算层面**：直接暴力采用现货指数（如 NDX）对比基准日现货指数的原始涨跌幅模型。代码更少，逻辑更正宗，从根本上杜绝了浮点数多次乘除带来的精度丢失与逻辑撕裂。

---

## 7. 对冲数量计算的双轨推演算法

在量化沙盘中，当系统预设固定的人民币本金时，必须**极其精准地推算出敞口（Delta）闭合状态下，中美两市所需的委托股数**。系统针对不同类型的资产设计了两套独立的逆向物理推导模型，其核心锚点全部建立在 API 解析而来的物理 `对冲值 (Hedge)` 之上。

### 7.1 纯ETF与指数类（如 162411, 161127, 161125, 161130）

**算法逻辑：美股定 A 股**。由于此类资产成分高度单一，等效追踪效应极强，须保证美股股数的整数下探精准。

1. **计算临时拟合股数**：
   ```
   临时数量 = 投入金额 / LOF盘中实时交易价
   ```

2. **锁定美股实际委托头寸**：
   ```
   美股对冲数量 = ROUND(临时数量 / 对冲值)  （四舍五入，保底 1 股）
   ```

3. **反推锁定A股委托头寸**：
   ```
   LOF实际交易数量 = ROUND((美股对冲数量 × 对冲值) / 100) × 100  （A股受限100股倍数规则，依此凑整）
   ```

### 7.2 商品类（如 160719 黄金, 160723 原油）

**算法逻辑：A股定美股**。大宗商品 LOF 底层常由全球一篮子资产拼接转换，A 股为实际执行主端，需先强制凑齐境内手数，后用海外衍生品兜底。

1. **锁定A股实际委托头寸**：
   ```
   LOF实际交易数量 = ROUND((投入金额 / LOF盘中实时交易价) / 100) × 100
   ```

2. **被动派生美股委托头寸**：
   ```
   美股对冲数量 = ROUND(LOF实际交易数量 / 对冲值)  （四舍五入，保底 1 股）
   ```

*(注：大宗商品本身缺乏直接对冲比参数，其底层物理"对冲值"将在系统中，通过 `Woody动态校准值 ÷ 基金实际合约仓位比例` 重建并自动固化。)*

---

## 8. 取价策略：卖一 vs 买一

根据目前代码中的实现逻辑，A股 LOF 价格和 IB美股 ETF 价格采用了不同的取价策略，具体如下：

### 8.1 A股 LOF 价格：优先采用【卖一价 (Ask 1)】，兜底采用【最新成交价】

无论是通过哪个行情引擎（银河QMT、通达信、国金QMT，还是新浪API兜底），系统在获取 LOF 价格时，都严格遵循了**优先取卖一价**的防守型原则。

- **常规情况**：直接使用盘口的**卖一价 (Ask 1)**。因为对于溢价套利而言，按卖一价买入 LOF 才是你能立刻拿到筹码的真实成本，这样计算出的溢价率更加保守和真实。
- **涨停或无卖盘**：代码中有一层防呆机制，如果遇到**卖一价 == 0**（比如基金涨停封板，卖盘被清空），此时系统会自动降级，兜底使用**最新成交价 (Last Price / Now)** 作为计算基准。

*(代码出处：LOF02_fetch_trade_data.py 中的 LOFPriceReader 类，各引擎回调中均有 `price_to_use = float(snap.get('Sell1', 0))`，`if price_to_use == 0: price_to_use = float(snap.get('Now', 0))` 等类似判断)*

### 8.2 IB 的美股 ETF 价格（夜盘）：采用【买一价 (Bid 1)】

在系统展示和推演时，从 IB API (外盘) 拿到的美股 ETF 价格采用的是**买一价 (Bid)**。

在前端轮询 IB 数据更新的 JS 代码中，专门提取了 bid 字段映射到页面及沙盘的计算中：
```javascript
prices.GLD && prices.GLD.bid ? prices.GLD.bid.toFixed(2) : '-'
```

取"买一价"是因为在夜盘对冲（卖出/做空底层 ETF）时，你是以 Bid 价把筹码抛给买方的，这也是一种贴近实际交易滑点的防守型取价方式。

### 8.3 总结

```
LOF 实时价格：卖一价（涨停时降级为最新成交价）
IB ETF 实时价格：买一价
```

这套取价逻辑在数学上构成了**"买入 A股 LOF (看卖一) + 卖空 美股 ETF (看买一)"** 的严谨对冲闭环，把盘口点差（Spread）的摩擦成本已经包含在溢价率的计算当中了，因此面板上显示的溢价率是没有水分的"净利润率"。

---

## 9. 校准值的采样机制

### 9.1 校准值不是每分钟定时任务

校准值的触发机制是：**当网页请求导致两边行情被刷新**，且两边行情满足严格对齐条件时，就把这一次的 `factor` 记为当天的一个样本。

对齐条件来自 `php/stock/stockref.php` 的 `CheckAdjustFactorTime($etf_ref)`：

1. 两边都有数据、且都不在盘前/盘后（`IsExtendedMarket()`）
2. 日期相同
3. **HH:MM 完全相同**（注意：是分钟级严格相等，不是"差不多"）

并且 `FundPairReference` 在构造阶段就会做判断，满足就调用 `AverageCalibraion()` 写样本（`php/stock/fundpairref.php`）。

### 9.2 为什么你看到它"像每分钟一次"？

因为抓新浪行情有**一分钟级限频/本地缓存**（`StockNeedFile(..., SECONDS_IN_MIN)`）：同一分钟内重复请求通常拿不到"新一分钟时间戳"的行情，自然也不会产生新的样本。

另外，woody 里"非盘前盘后"的判定窗口对美股/美期写死为 **09:30–16:00（美东）**（`IsExtendedMarket()` 使用这个窗口来判断是否 extended）。

### 9.3 补充：用户访问触发校准

**"用户访问触发校准"并不是额外条件**，它只是"让行情去刷新"的触发器；真正能不能写入样本，仍然要满足"同日同分钟 + 常规时段"的严格对齐条件。也正因为是"访问驱动"，当 A 股长假（比如春节）大家不看盘、网站没人访问时，**校准采样次数会明显下降**——这恰恰是合理的：A 股不交易时再去算"期货校准实时估值"本身就没有实际意义。

### 9.4 16:00 怎么算均值？

它不是等到收盘再算，而是每次触发时做"运行中均值更新"。

`php/sql/sqldailytime.php` 的 `CalibrationSql::WriteDailyAverage()`：

- 当天第一条：直接插入 `avg = factor`，`num = 1`
- 之后每条：增量更新算术均值：

```
newAvg = (num * oldAvg + factor) / (num + 1)
num += 1
```

还有一个细节：如果 `abs(oldAvg - factor) <= MIN_FLOAT_VAL`，它会认为"没有变化"，不会让 `num+1`（避免超高频重复样本把 `num` 撑爆）。

因此：数据库里 `calibrationhistory.close` **永远都是"截至当前时刻的当日均值"**，不需要保留每分钟明细，更不需要到 16:00 再回放计算。

---

## 附录：关键源码文件清单

### Woody 系统（PHP）

| 文件 | 功能 |
|-----|------|
| `php/stock/qdiiref.php` | QDII 单参考标的型估值 |
| `php/stock/holdingsref.php` | Holdings 型多持仓估值 |
| `php/stock/fundpairref.php` | 配对标的校准值计算 |
| `php/stock/stockref.php` | 行情刷新与校准触发 |
| `php/sql/sqldailytime.php` | 校准值增量均值更新 |
| `php/ui/calibrationhistoryparagraph.php` | 校准值历史展示 |

### 本系统（Python）

| 文件 | 功能 |
|-----|------|
| `backend/core/arbcore/calculators/static_valuation.py` | 静态估值计算 |
| `backend/core/arbcore/calculators/valuation_math.py` | 核心估值数学引擎 |
| `backend/core/arbcore/config/valuation_mapping.py` | 估值方法映射配置 |
| `backend/services/fund_service.py` | 基金估值服务 |

---

## 更新日志

- 2026-06-08: 初始版本，融合 `docs/009_估值_校准值、对冲值.md` 和 `docs/010_实时估值的源码真相与校准值计算.md` 的核心内容
