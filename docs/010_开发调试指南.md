# 程序3：ArbDashboard - 开发调试指南

## 一、环境搭建

### 1.1 系统要求

- **操作系统**：Windows 10/11（支持 Linux/Mac）
- **Python**：3.11
- **Node.js**：18+
- **npm**：9+

### 1.2 后端环境搭建

```bash
# 1. 创建虚拟环境
cd D:\Study\arbTest\ArbDashboard
python -m venv .venv

# 2. 激活虚拟环境
.venv\Scripts\activate

# 3. 安装依赖
cd backend
pip install -r requirements.txt

# 4. 验证安装
python -c "import fastapi; print(f'FastAPI {fastapi.__version__}')"
```

### 1.3 前端环境搭建

```bash
# 1. 进入前端目录
cd D:\Study\arbTest\ArbDashboard\frontend

# 2. 安装依赖
npm install

# 3. 验证安装
npm list vue
```

---

## 二、启动与运行

### 2.1 一键启动（推荐）

```bash
# Windows
D:\Study\arbTest\ArbDashboard\start_dashboard.bat
```

### 2.2 手动启动后端

```bash
# 方式1：直接运行
cd D:\Study\arbTest\ArbDashboard\backend
python main.py

# 方式2：使用 uvicorn
cd D:\Study\arbTest\ArbDashboard\backend
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

**访问地址**：
- API：http://localhost:8000
- 文档：http://localhost:8000/docs

### 2.3 手动启动前端（开发模式）

```bash
cd D:\Study\arbTest\ArbDashboard\frontend
npm run dev
```

**访问地址**：
- 前端：http://localhost:5173

---

## 三、常见问题排查

### 3.1 后端启动失败

#### 错误1：ModuleNotFoundError

**现象**：
```
ModuleNotFoundError: No module named 'paramiko'
```

**解决**：
```bash
pip install paramiko
```

#### 错误2：端口被占用

**现象**：
```
[Errno 10048] address already in use
```

**解决**：
```powershell
# 查找占用 8000 端口的进程
netstat -ano | findstr :8000

# 杀死进程（替换 PID）
Stop-Process -PID <PID> -Force
```

#### 错误3：数据库路径错误

**现象**：
```
sqlite3.OperationalError: no such table: unified_fund_history
```

**解决**：
- 确认数据库路径：`D:\Study\arbTest\database\arb_master.db`
- 检查 `backend/main.py` 第41行路径配置

#### 错误4：通达信连接失败

**现象**：
```
ConnectionRefusedError: [WinError 10061] 无法建立连接，因为目标计算机主动拒开了它
```

**解决**：
1. 确认通达信已启动
2. 确认通达信插件已挂载（默认端口 7709）
3. 检查防火墙设置

#### 错误5：通达信 TQ 接口未正确初始化（ctypes 回调异常）

**现象**：
控制台终端疯狂打印如下报错并刷屏：
```
Exception ignored on calling ctypes callback function: <bound method tq._data_callback_transfer of <class 'tqcenter.tq'>>
Traceback (most recent call last):
  File "D:\new_tdx_test\PYPlugins\user\tqcenter.py", line 950, in _data_callback_transfer
    run_id = cls._get_run_id()
             ^^^^^^^^^^^^^^^^^
  File "D:\new_tdx_test\PYPlugins\user\tqcenter.py", line 571, in _get_run_id
    raise RuntimeError("TQ数据接口未正确初始化")
RuntimeError: TQ数据接口未正确初始化
```

**根因分析**：
`tqcenter` 底层是 C/C++ 封装的接口，调用 `tq.initialize()` 时强制要求传入通达信插件安装目录下 `tqcenter.py` 的绝对物理路径。若使用了 `tq.initialize(__file__)` 或其他非通达信目录路径，将触发底层完整性校验失败。

**关键细节**：`_data_callback_transfer` 是注册在 C 层的 **ctypes 回调**，它调用 `cls._get_run_id()` 是通过 **classmethod 描述符协议**。因此普通的 `tq._get_run_id = safe_func` 只 patch 了实例属性，**无法拦截** `cls._get_run_id()` 的调用。必须同时 patch 类字典：`type(tq)._get_run_id = staticmethod(safe_func)`。

**修复策略（多层防御，已在 `main.py` 实现）**：

1. **初始化路径修正**：将所有 `tq.initialize(__file__)` 修正为正确路径：
   ```python
   tdx_plugin_path = os.path.join(tdx_api_path, 'tqcenter.py')
   tq.initialize(tdx_plugin_path)
   ```

2. **拦截重复初始化**：用 `safe_initialize()` 替换 `tq.initialize`，防止后续模块重复初始化导致路径覆盖。

3. **Patch `_get_run_id`（实例+类双层）**：
   ```python
   tq._get_run_id = safe_get_run_id           # 实例层 patch
   type(tq)._get_run_id = staticmethod(safe_get_run_id)  # 类字典 patch
   ```

4. **Patch `_data_callback_transfer` 回调本体**：彻底在回调入口拦截 RuntimeError，且只记录一次日志，避免刷屏：
   ```python
   _orig_callback = getattr(tq, '_data_callback_transfer', None)
   tq._data_callback_transfer = safe_data_callback_transfer
   type(tq)._data_callback_transfer = safe_data_callback_transfer
   ```

**注意**：如果以上 Python 层 patch 仍无法拦截（因为 ctypes 回调可能已在 DLL 加载时绑定了原始 C 函数指针），则需确保通达信客户端已启动并正常运行。

#### 错误5：通达信 TQ 接口未正确初始化（ctypes 回调异常）

**现象**：
当启动程序3且通达信客户端未打开时，控制台终端疯狂打印如下报错并刷屏：
```
Exception ignored on calling ctypes callback function: <bound method tq._data_callback_transfer of <class 'tqcenter.tq'>>
Traceback (most recent call last):
  File "D:\new_tdx_test\PYPlugins\user\tqcenter.py", line 950, in _data_callback_transfer
    run_id = cls._get_run_id()
             ^^^^^^^^^^^^^^^^^
  File "D:\new_tdx_test\PYPlugins\user\tqcenter.py", line 571, in _get_run_id
    raise RuntimeError("TQ数据接口未正确初始化")
RuntimeError: TQ数据接口未正确初始化
```

**底层技术背景与知识科普**：

1. **什么是通达信 Python 插件架构（沙箱运行 vs. 外部运行）**：
   - 通达信的 `tqcenter` 插件底层主要是用 C/C++ 动态链接库（DLL）编写的。
   - 当你在通达信软件内运行脚本时，Python 解释器是内嵌在通达信软件内部的，环境参数完全自动传递。
   - 当你在 CMD、PowerShell 等外部环境中独立启动程序3（FastAPI 网页服务）时，你脱离了通达信的沙箱环境。此时，`tqcenter.py` 底层的 C 库需要通过文件系统和特定绝对路径，反向搜寻并连接本地正在运行的通达信客户端程序。

2. **为什么普通的 Python 猴子补丁（Monkey Patch）会失效？**：
   - **C 语言 ctypes 回调机制**：`tqcenter.py` 底层的 DLL 驱动会将回调函数（例如 `_data_callback_transfer`）直接注册到 C/C++ 内存的底层回调链上。
   - 当 DLL 内部状态发生改变或重连时，DLL 会直接跳转到最初绑定该函数时的 **C 语言指针**。
   - 这意味着，如果你在 Python 中简单地写 `tq._data_callback_transfer = new_func`，DLL 的 C 层根本无法感知这个 Python 层的属性替换，它依然会调用原始注册的 `_data_callback_transfer`！
   - 同理，`_get_run_id` 是一个被 `@classmethod` 修饰的类方法。如果在回调中使用了 `cls._get_run_id()` 形式调用，它会直接通过类字典描述符协议获取方法。普通的实例 patch `tq._get_run_id = safe_func` 会因为被类字典劫持而**完全失效**！

3. **双重 Patch 深度拦截方案**：
   为了防止在通达信没有登录时 DLL 依然在后台死循环尝试重连并疯狂刷屏报错，系统在 `main.py` 中部署了深层拦截：
   - **类字典拦截**：利用 `type(tq)._get_run_id = staticmethod(safe_get_run_id)` 绕过类属性查找机制，强制所有的 `cls._get_run_id()` 回归到安全代理。
   - **回调外衣包裹**：在 C 库尚未发起回调注册前，将 `tq._data_callback_transfer` 重新替换为带有异常捕捉（`try-except`）的安全代理函数，在抛出第一个 `RuntimeError` 时打印提示，之后所有报错直接静默，防止控制台刷屏。

---

#### 错误6：未检测到已就绪的通达信(TDX)交易账户（启动熔断退出）

**现象**：
双击运行 `start_dashboard.bat` 启动程序3时，后端终端（ArbNext Backend 窗口）输出一个极其显眼的红字警告框，随后后端进程主动崩溃退出（因为 `start_dashboard.bat` 使用了 `cmd /k`，所以命令行窗口会保留在桌面供你阅读报错）：
```
================================================================================
🚨🚨🚨 致命错误：未检测到已成功连接的【通达信】交易账户！ 🚨🚨🚨
================================================================================
👉 原因分析：
   1. 本地【通达信客户端 (TdxW.exe)】尚未启动，或未成功登录交易账号。
   2. tqcenter 插件无法取得有效的 stock_account 句柄。
...
```

**设计意图与主从隔离逻辑**：

为了让程序3彻底在本地挑起交易重任，并能随时安全稳定地调用通达信，系统采用了**严密的启动熔断机制**：

1. **主从模式判定**：
   - 看板在启动时，会尝试探测本地 `5000` 端口。如果发现程序1 (LOFarb) 正在运行，它将自动进入 **从机模式 (Slave)**，禁用自身的通达信接口，完全依靠程序1去同步数据和处理交易，防止两个程序同时写入通达信 DLL 发生文件锁死或冲突崩溃。此时程序3能顺利开启。
2. **独立运行检验（启动熔断）**：
   - 如果程序1没有运行，说明程序3将被当作**核心交易主程序**独立运行。
   - 此时，程序3会非常严格地检测通达信连接：它会在 `sys.exit(1)` 前通过 `tq.stock_account()` 获取本地交易账号句柄。
   - 如果你的通达信客户端没有启动，或者尚未成功登录（即获取到的账号句柄为 `None` 或 `0`），程序3会认定这属于**交易风险环境**。它不会在后台静默失败，而是立刻弹出上方的**巨幅警告框**，并强行中断并退出，阻止不安全的后台启动。

**日常排查及正确启动顺序**：
1. **第一步**：先双击打开你电脑上的【通达信交易客户端】，输入账号密码并**成功登录**进去（保持通达信在后台运行）。
2. **第二步**：双击运行主目录下的 `start_dashboard.bat`。此时，后端程序会瞬间检测到通达信的 stock_account 句柄，绿灯放行，正常启动看板！

---

### 3.2 前端启动失败

#### 错误1：端口被占用

**现象**：
```
Port 5173 is already in use
```

**解决**：
```bash
# 指定其他端口
npm run dev -- --port 5174
```

#### 错误2：依赖安装失败

**现象**：
```
npm ERR! code ERESOLVE
```

**解决**：
```bash
# 清除缓存
npm cache clean --force

# 删除 node_modules 重新安装
rm -rf node_modules package-lock.json
npm install
```

---

### 3.3 数据不显示

#### 问题1：主页"现价"列全部显示 `-`

**原因**：程序连接到了错误的数据库（空白数据库）

**解决**：
- 检查 `backend/main.py` 第41行：
```python
# 错误路径
root_db_path = os.path.join(project_root, "database", "arb_master.db")

# 正确路径
root_db_path = os.path.abspath(os.path.join(project_root, "..", "database", "arb_master.db"))
```

#### 问题2：深度分析页面曲线不显示

**原因**：分时采样服务未运行或采样数据为空

**解决**：
1. 检查后端日志是否有采样服务启动信息
2. 确认当前时间在交易时段（9:30-11:30, 13:00-15:00）
3. 等待至少 1 分钟让采样服务写入数据
4. 检查数据库 `fund_intraday_quotes` 表是否有数据

```sql
SELECT COUNT(*) FROM fund_intraday_quotes WHERE date = '2026-06-08';
```

#### 问题3：unified_fund_history 表为空

**原因**：未运行数据同步脚本

**解决**：
1. 在"数据管理"页面点击"立即运行 LOF011"
2. 或手动运行：
```bash
cd D:\Study\arbTest\LOFarb
python LOF011_daily_updater.py
```

---

### 3.4 自动交易引擎问题

#### 问题1：引擎无法启动

**现象**：点击"启动引擎"按钮无反应

**解决**：
1. 检查后端日志是否有错误
2. 确认 `backend/private/trade_executor.py` 文件存在
3. 确认通达信已登录

#### 问题2：规则触发但不下单

**原因**：冷却时间未过或仓位已达上限

**解决**：
1. 检查后端日志中的冷却记录
2. 检查持仓是否达到 `max_pos_wan` 限制

#### 问题3：自动交易引擎的“开启/暂停”状态代表什么？

**解答**：
*   **引擎指代**：这指的是后台运行的自动网格盯盘与自动交易执行引擎（`AutoTradeRunner`）。
*   **状态说明**：
    *   **开启状态**：系统会实时扫描所有激活的策略规则（折溢价阈值等），一旦触发买卖信号且启用了真实报单，系统将**自动调用底层交易通道（通达信/QMT）执行真实下单委托**。
    *   **暂停状态**：系统处于挂起休眠状态，**不会执行任何自动下单或交易动作**，此时系统仅用于只读展示，以保障实盘安全。您随时可以在策略看板或右上角手动一键切换。
*   **物理隔离设计的意义**：数据抓取是高频且只读的，而交易下单具有实盘资金风险。物理隔离设计可以保证：即使行情数据如何跳动，只要自动交易引擎关闭，就绝不会发生任何实盘误交易，保障系统在不交易时的绝对安全。

#### 问题4：顶部行情状态中“美港: IB/富途”代表什么？两个都开启时程序到底用哪个？

**解答**：
*   **首选主通道（IB）**：当 IB 和富途都开启并成功连通时，顶部显示为 `美港: IB/富途`。此时，系统会把**所有美股行情请求发送给 IB**，因为它是您的主数据源。富途此时处于 **“热备份（Hot Standby）”** 状态，在线但静默，以节省富途 API 请求频次。
*   **无缝备用切换（富途）**：一旦 IB 客户端被关闭、断开连接，或者在获取价格时发生超时报错，系统会**瞬间、无缝地将行情抓取请求切换到富途**，由富途接管，确保您的实时估值和折溢价套利计算绝不断网。
*   **具体显示规则**：
    *   若只显示 `美港: IB`，说明只有 IB 在线，富途 OpenD 未开启。
    *   若只显示 `美港: 富途`，说明 IB 未开启或已断开，系统已自动降级至富途通道读取数据。
    *   若两者均未开启，系统显示 `美港: 未连接`。

---

## 四、Debug 技巧

### 4.1 后端日志查看

**日志位置**：`backend/logs/app.log`

**日志级别**：
- `INFO`：正常运行信息
- `WARNING`：警告信息
- `ERROR`：错误信息
- `DEBUG`：调试信息（开发模式）

### 4.2 使用 Swagger UI 测试 API

**地址**：http://localhost:8000/docs

**优点**：
- 可视化所有 API 接口
- 可直接在线测试
- 查看请求/响应格式

### 4.3 数据库查询验证

**使用 SQLite 浏览器**：
1. 下载 DB Browser for SQLite
2. 打开 `D:\Study\arbTest\database\arb_master.db`
3. 执行 SQL 查询验证数据

**示例查询**：
```sql
-- 查看基金最新数据
SELECT * FROM fund_data WHERE fund_code = '162411';

-- 查看历史数据
SELECT * FROM unified_fund_history WHERE fund_code = '162411' ORDER BY date DESC LIMIT 10;

-- 查看分时数据
SELECT * FROM fund_intraday_quotes WHERE fund_code = '162411' AND date = '2026-06-08';
```

### 4.4 前端调试

**使用浏览器开发者工具**：
1. 按 F12 打开开发者工具
2. 查看 Network 标签：检查 API 请求/响应
3. 查看 Console 标签：查看 JavaScript 错误
4. 查看 Vue DevTools：查看组件状态

**Vue DevTools 安装**：
```
Chrome 扩展商店搜索 "Vue.js devtools"
```

### 4.5 常见日志解读

后端日志（`backend/logs/app.log`）中会输出大量运行状态信息，以下是几条最常见的日志及其含义：

#### `[INDEX-SKIP] skip_qdii_asia_index=1，仅抓取 X 个指数（跳过QDII亚洲/国内LOF）`

**含义**：Settings 页面的「跳过QDII亚洲指数」开关为开启状态（默认=1）。

Dashboard 生成数据时，只保留黄金原油 + QDII欧美 + 白银 三个分类的基金，仅抓取它们 `related_index` 对应的指数行情（约 24 个），跳过 QDII亚洲和国内LOF 分类的指数，减少 API 调用。

**「为什么 HSI 还在获取？」**
因为 `160125 南方香港LOF` 属于 **QDII欧美** 分类（不是 QDII亚洲），它的 `related_index=HSI`，所以 HSI 在保留名单里。跳过逻辑是按基金分类过滤，不是按指数名称过滤。

此开关可在 Settings 页面切换（API: `GET/POST /api/config/app_settings/skip_qdii_asia_index`），设为 `0` 则抓取全部指数。

---

#### `[INDEX-TENCENT] 获取指数 HSI 价格: 23453.240 涨跌幅: -0.69%`

**含义**：通过腾讯行情接口 (`qt.gtimg.cn`) 成功获取到该指数（本例为恒生指数 HSI）的实时价格和涨跌幅。

腾讯拿不到的数据会自动走新浪接口兜底。港股指数（HSI/HSCEI/HSTECH）和非标港股指数（HSCI/HSSI/HSMI 等）会通过别名映射到标准指数再查询。

---

#### `Dashboard数据生成完成，共 X 只基金`

**含义**：`get_unified_dashboard_data()` 函数完成了一次 Dashboard 数据快照生成。

**X 的含义**：全量 88 只基金经过多层筛选后的最终结果数：

| 筛选层级 | 说明 |
|---------|------|
| ① 全量基金 | unified_fund_list 全部 88 只 |
| ② 分类筛选 | 你勾选的分类（如黄金原油） |
| ③ 自选筛选 | 开启「我的自选」时进一步过滤 |
| ④ 溢价率阈值 | 开仓/平仓阈值区间（如 -0.5% ~ 2.0%） |
| **⑤ 最终结果** | **X** |

- **数字偏小正常**：阈值设得窄（如 -0.1% ~ 0.5%）或只选了少量分类，结果自然少
- **数字突降留意**：如果昨天 40 只今天变 10 只，可能某类数据源断了，需要检查

---

## 五、代码修改规范

### 5.1 修改后端代码

1. 修改 `backend/services/` 下的文件
2. 重启后端服务（Ctrl+C 后重新运行）
3. 查看日志确认无错误

### 5.2 修改前端代码

1. 修改 `frontend/src/` 下的文件
2. 开发模式下自动热更新（HMR）
3. 生产模式需要重新构建：
```bash
cd frontend
npm run build
```

### 5.3 强制刷新缓存

**前端**：
- Windows：Ctrl + F5
- Mac：Cmd + Shift + R

---

## 六、性能优化建议

### 6.1 前端优化

1. **虚拟滚动**：大数据量表格使用虚拟滚动
2. **防抖节流**：搜索框输入使用防抖
3. **按需加载**：路由组件懒加载

### 6.2 后端优化

1. **数据库索引**：确保高频查询字段有索引
2. **连接池**：高频查询使用连接池
3. **缓存**：静态数据使用内存缓存

### 6.3 数据库优化

1. **定期清理**：删除 30 天前的分时数据
2. **定期备份**：每周备份数据库文件
3. **WAL 模式**：已启用，无需额外配置

---

## 七、开发工具推荐

### 7.1 后端开发

| 工具 | 用途 |
|------|------|
| VS Code | 代码编辑器 |
| Python 扩展 | 语法高亮、linting |
| Postman | API 测试 |
| DB Browser for SQLite | 数据库管理 |

### 7.2 前端开发

| 工具 | 用途 |
|------|------|
| VS Code | 代码编辑器 |
| Volar | Vue 3 语法支持 |
| Vue DevTools | 组件调试 |
| Chrome DevTools | 浏览器调试 |

---

## 八、Git 工作流

### 8.1 分支策略

```
main          # 生产环境代码
develop       # 开发分支
feature/xxx   # 功能分支
bugfix/xxx    # 修复分支
```

### 8.2 提交规范

```bash
# 格式：<type>(<scope>): <subject>

# 示例
git commit -m "feat(dashboard): 添加实时溢价率显示"
git commit -m "fix(fund_service): 修复现价数据错误"
git commit -m "docs(003): 更新数据库设计文档"
```

### 8.3 私有文件保护

**已配置 `.gitignore`**：
- `backend/private/`：私有模块
- `frontend/src/private/`：私有前端模块
- `*.pyc`：Python 字节码
- `__pycache__/`：Python 缓存
- `.env`：环境变量文件

---

## 十、前端筛选逻辑Bug修复（2026-06-10）

### 10.1 问题：选择分类后页面显示空白

**现象**：
用户在"全场套利机会实时雷达"页面选择"黄金原油"分类，并设定折价率阈值范围后，页面显示空白，没有任何基金数据。

**原因**：
筛选逻辑存在两个严重Bug：

**Bug #1：分类筛选不匹配**

数据库存储的category值是**中文**：
```
"黄金原油"、"纯ETF"、"QDII欧美"、"混合跨境"、"指数"、"QDII 亚洲"、"指数LOF"、"其他"、"国内LOF"、"白银"
```

前端复选框使用的是**英文key**：
```javascript
selectedCategories = ['gold_oil', 'qdii_us', 'qdii_asia', 'domestic_lof', 'silver']
```

**错误的筛选逻辑**（`frontend/src/views/Analysis.vue` 第853行）：
```javascript
// ❌ 错误：直接比较英文key和中文category，永远不匹配
data = data.filter((f: any) => selectedCategories.value.includes(f.category))
```

**Bug #2：折价率阈值筛选逻辑错误**

**错误的筛选逻辑**（第859行）：
```javascript
// ❌ 错误：筛选折价率在中间值的基金
data = data.filter((f: any) => f.rt_premium >= lower && f.rt_premium <= upper)
```

**问题**：这筛选的是折价率在 -0.5% 到 2.0% **之间**的基金，但折价套利的需求是：
- **折价越大越好**：折价率 ≤ 开仓阈值（如 ≤ -0.5%）→ 低价买入机会
- **溢价过高要平仓**：溢价率 ≥ 平仓阈值（如 ≥ 2.0%）→ 利润空间小，平仓

### 10.2 解决方案

**1. 添加分类映射表**（在组件script部分定义）：
```javascript
// 雷达筛选相关
const selectedCategories = ref<string[]>([])
const premiumThreshold = ref(-0.5)
const premiumUpperThreshold = ref(2.0)

// 分类映射：前端显示名称 → 数据库category值
const categoryMap: Record<string, string[]> = {
  'gold_oil': ['黄金原油'],
  'qdii_us': ['纯ETF', 'QDII欧美', '混合跨境', '指数'],
  'qdii_asia': ['QDII 亚洲'],
  'domestic_lof': ['指数LOF', '其他', '国内LOF'],
  'silver': ['白银']
}
```

**2. 修改筛选逻辑**：
```javascript
const fetchDashboard = async () => {
  try {
    const res = await axios.get('/api/dashboard')
    let data = res.data.data
    
    // 分类筛选（使用映射表）
    if (selectedCategories.value && selectedCategories.value.length > 0) {
      // 收集所有选中的分类对应的数据库category值
      const allowedCategories = new Set<string>()
      for (const catKey of selectedCategories.value) {
        const mappedCats = categoryMap[catKey] || []
        for (const cat of mappedCats) {
          allowedCategories.add(cat)
        }
      }
      data = data.filter((f: any) => allowedCategories.has(f.category))
    }
    
    // 折价率阈值筛选：折价小于开仓阈值 或 溢价大于平仓阈值
    const lower = premiumThreshold.value
    const upper = premiumUpperThreshold.value
    data = data.filter((f: any) => f.rt_premium <= lower || f.rt_premium >= upper)
    
    opportunityData.value = data
  } catch (e) {}
}
```

### 10.3 映射关系详解

| 前端key | 数据库category值 | 说明 |
|---------|-----------------|------|
| `gold_oil` | `黄金原油` | 黄金原油分类（10只基金） |
| `qdii_us` | `纯ETF`、`QDII欧美`、`混合跨境`、`指数` | QDII欧美分类（11只基金，4个category） |
| `qdii_asia` | `QDII 亚洲` | QDII亚洲分类（16只基金） |
| `domestic_lof` | `指数LOF`、`其他`、`国内LOF` | 国内LOF分类（0只基金，3个category） |
| `silver` | `白银` | 白银分类（1只基金） |

**注意**：
- QDII欧美Tab包含4个category：纯ETF、QDII欧美、混合跨境、指数
- 国内LOF Tab包含3个category：指数LOF、其他、国内LOF
- 其他Tab只包含1个category

### 10.4 雷达页面默认行为优化（2026-06-10 更新）

**问题1：默认显示所有基金（页面慢）**

**现象**：
页面加载后默认显示全部基金（即使设定了阈值-3%到3%，仍然显示了溢价18.39%的基金），页面响应慢。

**原因**：
- 默认不选任何分类时，筛选逻辑跳过分类筛选，显示全部基金
- 折价率阈值筛选逻辑错误（显示两端值而非中间值）

**解决方案**：
修改筛选逻辑，默认不选任何分类时，页面显示空列表。用户必须主动选择分类才会显示数据。

```javascript
// 2. 分类筛选（必须选择至少一个分类）
if (!selectedCategories.value || selectedCategories.value.length === 0) {
  data = [] // 未选择分类时，显示空
} else {
  const allowedCategories = new Set<string>()
  for (const catKey of selectedCategories.value) {
    const mappedCats = categoryMap[catKey] || []
    for (const cat of mappedCats) {
      allowedCategories.add(cat)
    }
  }
  data = data.filter((f: any) => allowedCategories.has(f.category))
}
```

**效果**：
- 默认：空列表（页面响应快）
- 选择"黄金原油"：显示黄金原油分类中满足条件的基金
- 选择"黄金原油" + "我的自选"：交集关系（既属于黄金原油，又在自选列表中）

### 10.5 "我的自选"Tab功能（2026-06-10 新增）

**功能说明**：
在雷达页面添加"★ 我的自选"Tab，用户可以快速标记关注的基金，并与分类筛选形成**交集关系**。

**添加的筛选控件**（第13-17行）：
```html
<!-- 我的自选Tab -->
<n-checkbox v-model:checked="showWatchlistOnly" size="small">
  <span style="font-weight: bold; color: #f59e0b;">★ 我的自选</span>
</n-checkbox>
```

**添加的响应式变量**（第464-465行）：
```javascript
const showWatchlistOnly = ref(false) // 我的自选Tab
const watchlist = ref<string[]>(JSON.parse(localStorage.getItem('watchlist') || '[]')) // 自选基金列表
```

**添加的切换函数**（第482-490行）：
```javascript
const toggleWatchlist = (code: string) => {
  const index = watchlist.value.indexOf(code)
  if (index > -1) {
    watchlist.value.splice(index, 1)
  } else {
    watchlist.value.push(code)
  }
}
```

**雷达表格添加★列**（第1025-1031行）：
```javascript
{
  title: '★', key: 'watchlist', width: 40, align: 'center' as const,
  render: (r: any) => h(NIcon, {
    size: 18, color: watchlist.value.includes(r.fund_code) ? '#f59e0b' : '#ddd',
    style: 'cursor: pointer;',
    onClick: (e: MouseEvent) => { e.stopPropagation(); toggleWatchlist(r.fund_code) }
  }, { default: () => watchlist.value.includes(r.fund_code) ? h(Star) : h(StarOff) })
}
```

**导入Star和StarOff图标**：
```javascript
import { RefreshCw, Zap, ArrowLeft, Star, StarOff } from 'lucide-vue-next'
```

**使用方式**：
1. 勾选"★ 我的自选" → 显示所有自选基金
2. 勾选"★ 我的自选" + "黄金原油" → 只显示黄金原油分类的自选基金
3. 点击雷达表格中的★图标 → 快速添加/移除自选

**数据持久化**：
自选列表自动保存到localStorage，刷新页面不丢失。

### 10.6 折价率阈值筛选逻辑修正（2026-06-10 更新）

**问题**：
原筛选逻辑筛选的是**两端值**（折价率≤-3%或≥3%），但用户需要的是**中间值**（折价率在-3%到3%之间）。

**错误的筛选逻辑**（第903行）：
```javascript
// ❌ 错误：筛选折价率在两端值的基金
data = data.filter((f: any) => f.rt_premium <= lower || f.rt_premium >= upper)
```

**问题**：
- 这筛选的是折价率≤-3%或≥3%的基金（两端值）
- 导致显示了溢价18.39%的基金（不满足-3%到3%的条件）

**正确的筛选逻辑**（第903行）：
```javascript
// ✅ 正确：筛选折价率在中间值的基金
const lower = premiumThreshold.value
const upper = premiumUpperThreshold.value
data = data.filter((f: any) => f.rt_premium >= lower && f.rt_premium <= upper)
```

**效果**：
- 阈值-3%到3%：显示折价率在-3%到3%之间的基金
- 不会显示溢价18.39%的基金（超出阈值范围）

### 10.7 完整筛选逻辑（最终版）

```javascript
const fetchDashboard = async () => {
  try {
    const res = await axios.get('/api/dashboard')
    let data = res.data.data
    
    // 1. 我的自选筛选（交集关系）
    if (showWatchlistOnly.value && watchlist.value.length > 0) {
      data = data.filter((f: any) => watchlist.value.includes(f.fund_code))
    }
    
    // 2. 分类筛选（必须选择至少一个分类）
    if (!selectedCategories.value || selectedCategories.value.length === 0) {
      data = [] // 未选择分类时，显示空
    } else {
      const allowedCategories = new Set<string>()
      for (const catKey of selectedCategories.value) {
        const mappedCats = categoryMap[catKey] || []
        for (const cat of mappedCats) {
          allowedCategories.add(cat)
        }
      }
      data = data.filter((f: any) => allowedCategories.has(f.category))
    }
    
    // 3. 折价率阈值筛选：显示折价率在阈值范围内的基金
    const lower = premiumThreshold.value
    const upper = premiumUpperThreshold.value
    data = data.filter((f: any) => f.rt_premium >= lower && f.rt_premium <= upper)
    
    opportunityData.value = data
  } catch (e) {}
}
```

### 10.8 映射关系详解

| 前端key | 数据库category值 | 说明 |
|---------|-----------------|------|
| `gold_oil` | `黄金原油` | 黄金原油分类（10只基金） |
| `qdii_us` | `纯ETF`、`QDII欧美`、`混合跨境`、`指数` | QDII欧美分类（11只基金，4个category） |
| `qdii_asia` | `QDII 亚洲` | QDII亚洲分类（16只基金） |
| `domestic_lof` | `指数LOF`、`其他`、`国内LOF` | 国内LOF分类（0只基金，3个category） |
| `silver` | `白银` | 白银分类（1只基金） |

**注意**：
- QDII欧美Tab包含4个category：纯ETF、QDII欧美、混合跨境、指数
- 国内LOF Tab包含3个category：指数LOF、其他、国内LOF
- 其他Tab只包含1个category

### 10.9 筛选效果说明

| 操作 | 显示结果 |
|------|----------|
| 默认（不选分类） | 空列表 |
| 选择"黄金原油" | 显示黄金原油分类中折价率在阈值范围内的基金 |
| 选择"黄金原油" + "我的自选" | 显示黄金原油分类中，且在自选列表中的基金 |
| 选择"黄金原油" + 阈值-3%~3% | 显示黄金原油分类中折价率在-3%到3%之间的基金 |

### 10.10 相关文件

| 文件 | 路径 | 说明 |
|------|------|------|
| 前端页面 | `frontend/src/views/Analysis.vue` | 实时分析页面，包含筛选逻辑 |
| 基金服务 | `backend/services/fund_service.py` | `get_unified_dashboard_data()` 函数 |
| 分类配置 | `arbcore/config/fund_categories.json` | 基金分类JSON配置 |
| 同步脚本 | `backend/scripts/sync_from_json_config.py` | JSON导入数据库脚本 |
| 数据库 | `d:\Study\arbTest\database\arb_master.db` | 统一数据库 |
| 数据库表 | `unified_fund_list` | 基金列表表（category字段） |

### 10.11 详细文档

如需更详细的修复说明和快速恢复指南，请参考：
- `BUG_FIX_ANALYSIS.md`（根目录，包含完整代码和恢复步骤）

---

## 十一、美股实时订阅白名单机制（解决 IB/富途 频控与刷屏）

### 9.1 设计背景
随着系统接入的跨境/混合基金越来越多（如 501312, 160644, 501225 等），这些基金的估值组合中引入了大量海外 ETF 成分股。
原系统会自动遍历所有基金下的成分股，并一股脑全部向盈透证券 (IB) 或富途发起实时订阅和看门狗历史数据拉取。
由于很多成分股并没有美股夜盘（Overnight）报价且我们并不需要对其进行对冲，IB 接口因为频繁返回空数据和发送超限，频繁报出 Pacing Violation (频控违规) 错误并使终端疯狂刷屏。

### 9.2 解决方案：自定义白名单
系统在数据中控台增加了一个**“美股订阅白名单 (IB/富途)”**配置功能，实现了以下架构逻辑：

1. **白名单数据库存储**：
   - 数据存储在 `data_source_config` 表中，标识为 `module = 'ib_config'` 且 `source_name = 'whitelist'`。
   - 配置字段 `config_json` 存储 JSON 格式的名单列表：`{"symbols": ["GLD", "USO", "XOP", "SLV", "SPY", "QQQ", "INDA"]}`。

2. **接口与内存同步**：
   - 后端提供了 `GET /api/config/ib_symbols` 与 `POST /api/config/ib_symbols/update` 两个 REST 接口用于读取与更新。
   - 当用户在前端保存了新的白名单后，后端会**即时更新内存中运行的 `ib_reader.symbols` 对象**。这意味着修改白名单**不需要重启后端服务**，更新瞬间在长连接中生效！

3. **读取优先级逻辑（在 `ib_reader.py` 轮询中实现）**：
   - `ib_reader.py` 后台轮询线程每次循环都会优先检测数据库中是否存在该白名单配置。
   - **如果存在白名单**：直接加载白名单中的代码作为订阅池，其他成分股将被**完全跳过**，不再向 IB 接口发起任何请求。
   - **如果留空（无白名单）**：系统会降级到原有的 YAML 提取逻辑，自动抓取 `lof_config.yaml` 中所有的底层美股。

### 9.3 前端使用指南
1. 打开左侧菜单的“数据管理”页面（Data.vue）。
2. 在左侧的**“脚本任务中控台”**正下方，找到“💡 美股订阅白名单 (IB/富途)”输入框。
3. 输入你需要关注的重点美股，用英文逗号分隔（例如：`GLD, USO, XOP, SLV, SPY, QQQ, INDA`）。
4. 点击**“保存白名单”**，前端会调用后端接口存入 SQLite。
5. 后端接收后，在控制台会即时输出 `🔄 已动态向 IBReader 推送新订阅白名单: [...]`。此时，系统立即对非白名单品种断流，彻底杜绝多余的 IB 请求！

---

*本文档描述程序3（ArbDashboard）的开发调试方法，最后更新：2026-06-10 21:53*
