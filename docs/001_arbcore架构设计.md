13# 整体架构设计说明 (Industrial Grade V2.2)

## 1. 系统愿景

LOF 基金套利监控系统旨在为专业投资提供一?*端到?*?*高可?*?*极低延迟**的套利决策支持环境过“云地协同数据采集与“多源布流行情引擎，确保在瞬息万变的套利窗口期提供最精准的溢价参考?

---

## 2. 核心架构逻辑：工业级“铁三角?

系统遵循 **BaseApp** 标准化设计规范，将整个套利生命周期拆分为四个核心阶段，形成稳固的流水线?

### 2.1 进食 (Data Siphon) - LOF011
- **职责**：全自动数据采集与历史追溯?
- **基座**：基?`BaseApp`，统丢配置加载与多线程追溯?
- **逻辑**：优先从云端 VPS 拉取高质量同步数据，若云端缺失则启动本地 API 降级采集?

### 2.2 思?(Calculators) - LOF012
- **职责**：纯本地静估值精算引擎?
- **基座**：利?`arbcore.calculators` 算法库?
- **特征**：零网络依赖，基于数据库 `fund_daily_factors` 进行毫秒级批量推演，产出 T-1 日真值?

### 2.3 跳动 (Real-time Hub) - LOF02
- **职责**：极速实时行情网关与 API 中心?
- **基座**：标准化 `BaseApp` 启动，内?IB、QMT、达信长连接池?
- **特征**：提?WebSocket 实时推，支持 A 股盘口五档行情与美股夜盘快照?

### 2.4 颜?(Renderer) - LOF03
- **职责**：高性能监控面板生成?
- **特征**：采?JS 侧沙盘推演技术，将复杂的计算逻辑前置到浏览器，实现服务器 0 压力的流畅监控?

---

## 3. 分层拓扑结构

```
+-----------------------------------------------------------+
|                   应用展示?(Presentation)               |
|      (HTML5 看板 / Admin 面板 / 移动端知 / JS 实时推演)     |
+-----------------------------+-----------------------------+
                              |
+-----------------------------v-----------------------------+
|                   业务服务?(Services)                   |
|  (Flask API / SocketIO 实时?/ TradeManager 交易网关)      |
+-----------------------------+-----------------------------+
                              |
+-----------------------------v-----------------------------+
|                   算法与核心层 (Core)                     |
|  (Static/Dynamic 计算引擎 / 汇率转换?/ 因子校准模型)        |
+-----------------------------+-----------------------------+
                              |
+-----------------------------v-----------------------------+
|                   数据持久?(Persistence)                |
|  (SQLite WAL 数据?/ Fund, Market, System 模块化管理器)    |
+-----------------------------------------------------------+
```

---

## 4. 关键抢术特?

### 4.1 模块化数据库管理?(Composition Over Inheritance)
重构后的 `DatabaseManager` 不再是臃肿的单体类，而是通过以下组合实现职责分离?
- **FundManager**：基金净值溢价篮子权重?
- **MarketManager**：汇率指数ETF 历史价?
- **SystemManager**：同步状态健康监测数据湖备份?

### 4.2 统一基座 (BaseApp)
扢有核心脚本均继承?`BaseApp`，获得以下开箱即用的能力?
- **NO_PROXY**：自动屏蔽系统代理，确保国内 API 请求不因梯子挂掉或被反爬?
- **Logger**：统丢格式的工业级日志，支持等级动态调整?
- **DB Connector**：自动定位项目根目录下的 `arb_master.db`?

### 4.3 交易接口防坍塌设?
`trade_manager.py` 采用延迟加载与异常熔断技术即使某个交易终端（?QMT）意外关闭，主服务仍能正常提供行情监控，仅在尝试下单时进行静默拦截并报警?

---

## 5. 数据丢致保?(Consistency)

系统通过 `fund_data` 超级宽表实现“动静结合：
- **T日盘?*：实时现价（Price）过 `Upsert` 机制覆写?
- **T日盘?*：官方净值（NAV）由 `LOF011` 填补?
- **校准逻辑**：`LOF012` 自动运行数据探针，如果发现汇率或基准点未对齐，将拒绝计算估并红色报警，从源头杜绝虚假溢价?

---
*本文档为 AI 助手与架构师共同制定的系统标准?
# arbcore - 核心函数?

## 概述

arbcore ?LOF 基金套利监控系统的核心函数库，提供了数据获取、处理和存储等基硢功能，为上层应用提供服务?

## 目录结构

```
arbcore/
├─┢ fetchers/        # 数据获取模块
?  ├─┢ data_fetcher.py         # 统一数据获取接口
?  ├─┢ woody_api_service.py    # 统一Woody API服务 (提取解析与备?
?  ├─┢ woody_telegram_client.py# Woody Telegram接口客户?
?  ├─┢ ib_reader.py            # IB盈实时行情与交易基座
?  └─┢ woody_web_crawler.py    # 网页爬虫
├─┢ database/        # 数据库模?
?  ├─┢ db_manager.py           # 数据库管理类
?  └─┢ import_basic_csv.py     # CSV 数据导入工具
```

## 核心模块

### 1. 数据获取模块 (fetchers)

#### data_fetcher.py

**功能**?
- 统一的数据获取接?
- 从多个数据源获取数据
- 支持汇率、LOF 基金、指数期货和美股数据
- 实现多源数据融合和故障转?

**主要方法**?
- `get_exchange_rate()`：获取汇率数?
- `get_lof_data()`：获?LOF 基金数据
- `get_index_data()`：获取指数数?
- `get_futures_data()`：获取期货数?
- `get_us_stock_data()`：获取美股数?

#### woody_web_crawler.py

**功能**?
- 网页爬虫，作?API 数据的备用来?
- ?Woody 网页获取数据
- 解析 HTML 页面提取数据

**主要方法**?
- `crawl_woody_data()`：爬?Woody 网页数据
- `parse_woody_html()`：解?Woody 网页 HTML

### 2. 数据库模?(database)

#### db_manager.py

**功能**?
- 数据库管理类
- 提供数据库连接和操作
- 实现表结构创建和数据操作
- 支持线程安全的数据库访问

**主要方法**?
- `initialize_database()`：初始化数据?
- `get_exchange_rate()`：获取汇率数?
- `get_futures_settlement()`：获取期货结算价
- `get_lof_data()`：获?LOF 基金数据
- `insert_exchange_rate()`：插入汇率数?
- `insert_futures_settlement()`：插入期货结算价
- `insert_lof_data()`：插?LOF 基金数据

#### import_basic_csv.py

**功能**?
- ?CSV 文件导入基础数据到数据库
- 支持数据清洗和转?
- 提供批量导入功能

**主要方法**?
- `import_csv_to_database()`：导?CSV 数据到数据库
- `clean_data()`：清洗数?
- `transform_data()`：转换数?

### 3. 工具函数

**功能**?
- 日期处理工具：处理交易日、假期等日期相关逻辑
- 数据处理工具：数据清洗转换等功能
- 配置管理工具：读取和管理配置文件

**实现方式**?
这些工具函数目前直接集成在各个模块中，没有单独的utils目录?

## 使用指南

### 1. 数据获取

```python
from arbcore.fetchers.data_fetcher import data_fetcher

# 获取汇率数据
rate = data_fetcher.get_exchange_rate('2023-07-01', 'CNY_USD')

# 获取 LOF 基金数据
data = data_fetcher.get_lof_data('501018')

# 获取指数数据
index_data = data_fetcher.get_index_data('000001.SH')

# 获取期货数据
futures_data = data_fetcher.get_futures_data('GC')

# 获取美股数据
us_stock_data = data_fetcher.get_us_stock_data('AAPL')
```

### 2. 数据库操?

```python
from arbcore.database.db_manager import DatabaseManager

# 初始化数据库管理?
db = DatabaseManager()

# 插入汇率数据
db.insert_exchange_rate('2023-07-01', 'CNY_USD', 6.5, 'SAFE')

# 插入期货结算?
db.insert_futures_settlement('2023-07-01', 'GC', 4500.0, 'CME')

# 插入 LOF 基金数据
db.insert_lof_data('2023-07-01', '501018', 1.5, 1.55, 1.52, 1.53, 1.54, 3.33, 1.5, 1.0, 'API')

# 查询数据
rate = db.get_exchange_rate('2023-07-01', 'CNY_USD')
futures = db.get_futures_settlement('2023-07-01', 'GC')
lof_data = db.get_lof_data('501018')
```

### 3. 工具函数使用

工具函数目前直接集成在各个模块中，例如：

- 日期处理函数集成?`data_fetcher.py` ?
- 配置管理函数集成在各个需要的模块?
- 数据处理函数集成在各个需要的模块?

具体使用方式请参考各个模块的文档?

## 数据流程

1. **数据获取**：过 data_fetcher 从多个数据源获取数据
2. **数据处理**：对获取的数据进行清洗和转换
3. **数据存储**：将处理后的数据存储到数据库
4. **数据查询**：从数据库查询数据供上层应用使用
5. **数据更新**：定期更新数据，确保数据的及时?

## 错误处理

- **数据源失?*：当主数据源失败时，自动切换到备用数据源
- **数据格式错误**：对获取的数据进行格式验证，处理格式错误
- **网络错误**：处理网络连接失败等错误
- **数据库错?*：处理数据库操作错误

## 性能优化

- **数据缓存**：对频繁访问的数据进行缓?
- **批量操作**：对数据库操作进行批量处?
- **并行处理**：对数据获取和处理进行并行处?
- **连接?*：使用连接池管理数据库连?

## 扩展指南

### 添加新的数据?

1. ?`data_fetcher.py` 中添加新的数据源获取方法
2. 实现数据源的错误处理和故障转?
3. 更新数据融合逻辑

### 添加新的数据库表

1. ?`db_manager.py` 中添加表结构创建代码
2. 添加相应的数据插入和查询方法
3. 更新数据库初始化逻辑

### 添加新的工具函数

1. 在相应的模块中添加新的工具函?
2. 确保函数的可重用性和通用?
3. 在模块的 `__init__.py` 文件中导出新的工具函数（如果霢要）

## 维护与支?

- **日志**：数据获取和处理过程会记录详细的日志
- **监控**：系统会监控数据获取和处理的状?
- **告警**：当数据获取或处理失败时，系统会发出告警

## 注意事项

- **API 访问限制**：使?API 时请注意遵守 API 提供商的访问限制
- **数据质量**：虽然系统会进行数据验证，但仍建议对重要数据进行人工验证
- **性能考虑**：处理大量数据时，请注意系统性能
- **安全?*：请妥善保管 API 密钥等敏感信?

arbcore 核心库为 LOF 基金套利监控系统提供了强大的基础功能，过合理使用这些功能，可以构建更加可靠和高效的套利策略

## 6. 项目概述

**arbTest** 是基金套利监控系统集合，采用**"大一统底层基座 (arbcore + SQLite WAL 模式)"**，共享同一个数据库。

| 项目 | 目录 | 状态 | 用途 |
|------|------|------|------|
| **程序3** | `ArbDashboard/` | 🔧 开发中 | **最终核心项目**（跨市场 QDII 看板与执行） |
| **程序1** | `LOFarb/` | ✅ 已完成 | LOF 基金折价套利（教学用） |
| **程序2** | `jsl/` | ⚠️ 未完成 | 集思录全市场监控（教学用） |
| **程序4** | `ETFRotate/` | ❌ 未使用 | ETF 轮动策略（旧代码） |

**底层基座**：`arbcore/` - 所有程序共享的核心库
**数据库**：`database/arb_master.db` (WAL 高并发模式，Single Source of Truth)
